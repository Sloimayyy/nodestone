package com.sloimay.nodestonecore.simulation.inputs

/**
 * A simulation input. Interface used along with simulation input ability interfaces
 * (PositionedInputs, IndexedInputs etc)
 * Can represent user inputs (levers, buttons etc) but that's up to the simulation to decide.
 */
interface SimInput